# COMPASS app — FastAPI backend
# Pinned to match what the Databricks Apps runtime (Python 3.11) provides today.
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2

# Databricks SDK + tracing
databricks-sdk>=0.81.0,<0.120.0
mlflow>=2.19.0,<3.0.0

# Lakebase (standard / full)
sqlalchemy==2.0.35
psycopg[binary]==3.2.3

httpx==0.27.2
